[Unit]
Description=OpenTelemetry Collector
After=network.target

[Service]
EnvironmentFile={{ install_dir }}/conf/config.yaml
ExecStart={{ install_dir }}/bin/otelcol $OTELCOL_OPTIONS
KillMode=mixed
Restart=on-failure
Type=simple
User=otel
Group=otel

[Install]
WantedBy=multi-user.target
