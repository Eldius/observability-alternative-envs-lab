---
- hosts: node1
  tasks:
    - name: Create Loki dashboard
      community.grafana.grafana_dashboard:
        grafana_url: http://192.168.0.68:3000
        state: present
        url_username: admin
        url_password: 123Senha
        commit_message: Configuring logs dashboard
        overwrite: true
        path: "{{ playbook_dir }}/../roles/grafana/files/dashboards/logs.json"
